/**
 * Oculta contenido visualmente pero lo mantiene accesible para lectores de
 * pantalla. Útil para etiquetas de iconos, encabezados de tablas, etc.
 *
 * Re-export directo del primitivo de Radix (no añade estilos, tipos ni lógica).
 */
export { Root as VisuallyHidden } from "@radix-ui/react-visually-hidden";
